services:
- type: web
  name: the-latte-bear-nails
  env: python
  region: oregon
  buildCommand: |
    pip install --upgrade pip
    pip install -r requirements.txt
    python manage.py collectstatic --noinput
  startCommand: |
    python manage.py migrate && gunicorn config.wsgi:application
  envVars:
    - key: SECRET_KEY
      generateValue: true
    - key: DEBUG
      value: "False"
    - key: EMAIL_HOST_USER
      sync: false
    - key: EMAIL_HOST_PASSWORD
      sync: false
    - key: MERCADOPAGO_ACCESS_TOKEN
      sync: false
    - key: SOCIALACCOUNT_GOOGLE_CLIENT_ID
      sync: false
    - key: SOCIALACCOUNT_GOOGLE_SECRET
      sync: false
    - key: DATABASE_URL
      fromDatabase:
        name: the-latte-bear-nails-db
        property: DATABASE_URL
databases:
- name: the-latte-bear-nails-db
  region: oregon
  plan: free